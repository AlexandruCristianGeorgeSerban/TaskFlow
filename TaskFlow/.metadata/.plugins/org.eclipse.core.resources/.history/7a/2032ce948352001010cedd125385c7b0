package com.taskflow.ui;

import com.taskflow.model.Task;

import javax.swing.table.AbstractTableModel;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.StringTokenizer;

/**
 * Table model for displaying Task objects in a JTable with filtering capability.
 */
public class TaskTableModel extends AbstractTableModel {
    private static final long serialVersionUID = 1L;

    // 4 coloane acum: Title, Description, Due Date, Completed
    private static final String[] COLUMN_NAMES = {
        "Title", "Description", "Due Date", "Completed"
    };

    private List<Task> tasks = new ArrayList<>();

    public TaskTableModel(List<Task> tasks) {
        this.tasks = tasks;
    }

    @Override
    public int getColumnCount() {
        return COLUMN_NAMES.length;  // va întoarce 4
    }

    @Override
    public String getColumnName(int column) {
        return COLUMN_NAMES[column];
    }

    @Override
    public int getRowCount() {
        return tasks.size();
    }

    @Override
    public Object getValueAt(int rowIndex, int columnIndex) {
        Task t = tasks.get(rowIndex);
        switch (columnIndex) {
            case 0: return t.getTitle();
            case 1: return t.getDescription();   // nou
            case 2: return new SimpleDateFormat("yyyy-MM-dd")
                             .format(t.getDueDate().getTime());
            case 3: return t.isCompleted();
            default: return null;
        }
    }

    @Override
    public Class<?> getColumnClass(int columnIndex) {
        switch (columnIndex) {
            case 2: return String.class;   // am formatat data ca String
            case 3: return Boolean.class;  // checkbox
            default: return String.class;
        }
    }

    public void setTasks(List<Task> tasks) {
        this.tasks = tasks;
        fireTableDataChanged();
    }
}
